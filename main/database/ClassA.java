package main.database;

public class ClassA { // possible name DatabaseReader
    // black box that reads JSON, converts to JSONObject
    // calls Class B with all JSONObjects read and then some kind of add() or
    //     save() method

    // Prevent user from instantiating this class
    private ClassA() {}
}
